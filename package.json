{
  "name": "my-extension",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && copyfiles -u 1 \"src/**/*.{html,css,json}\" dist"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "typescript": "^5.8.2",
    "@types/chrome": "^0.0.307"
  }
}
